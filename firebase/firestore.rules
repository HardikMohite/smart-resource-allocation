rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for hackathon demonstration - ENSURES ZERO DISPATCH ERRORS
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
